name: Build and Publish OpenMRS Clinic Config DB
on:
  push:
    branches:
      - main
      - 'release-*'
      - BAH-2744
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
    paths: 
      - package/docker/resources/omrs*.gz
      - package/docker/resources/configuration_checksums.zip
      - package/docker/configdb.Dockerfile
      - .github/workflows/build_publish_configdb.yaml
      - package/docker/resources/mysql:5.6/*
      - package/docker/configdb.mysql5.6.Dockerfile
  workflow_dispatch:
    
jobs:
  build-publish-docker:
    name: Build & Publish Docker Image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set env.ARTIFACT_VERSION
        run: |
          wget -q https://raw.githubusercontent.com/Bahmni/bahmni-infra-utils/main/setArtifactVersion.sh && chmod +x setArtifactVersion.sh
          ./setArtifactVersion.sh
          rm setArtifactVersion.sh
      - name: Docker - Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Decompress Files
        run: |
          gunzip package/docker/resources/omrs*.gz
          unzip package/docker/resources/configuration_checksums.zip -d package/docker/resources/
          gunzip package/docker/resources/mysql:5.6/omrs*.gz
          unzip package/docker/resources/mysql:5.6/configuration_checksums.zip -d package/docker/resources/mysql:5.6/
      - name: Docker - Build and push with mysql 5.7
        uses: docker/build-push-action@v3
        with:
          context: .
          file: package/docker/configdb.Dockerfile
          push: true
          tags: bahmni/openmrs-db:${{env.ARTIFACT_VERSION}},bahmni/openmrs-db:clinic-config-mysql5.7
      - name: Docker - Build and push with mysql 5.6
        uses: docker/build-push-action@v3
        with:
          context: .
          file: package/docker/configdb.mysql5.6.Dockerfile
          push: true
          tags: bahmni/openmrs-db:${{env.ARTIFACT_VERSION}},bahmni/openmrs-db:clinic-config-mysql5.6
